export interface Education {
  institution: string;
  period: string;
  degree: string;
  description: string;
}

export interface Certificate {
  institution: string;
  certificate: string;
  obtained: string;
  about: string;
}
